# Environment variables - Make sure to update these for production!
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Database Configuration
DATABASE_URL="postgresql://chatbot_user:Mirabel20.@localhost:5433/chatbot_db"

# Individual database components (if needed)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=chatbot_db
DB_USER=chatbot_user
DB_PASSWORD=Mirabel20.

# Authentication & Security
JWT_SECRET=honey-chatbot-secret-key-2024-production

# Server Configuration
NODE_ENV=development
PORT=3000

# Frontend Configuration (for CORS)
FRONTEND_URL=http://localhost:5173

# Admin Account (for initial setup)
ADMIN_EMAIL=admin@honey-health.com
ADMIN_PASSWORD=SecureAdminPass123!
ADMIN_NAME=System Administrator

# Default Agent Account (for initial setup)
AGENT_EMAIL=agent@honey-health.com
AGENT_PASSWORD=SecureAgentPass123!
AGENT_NAME=Support Agent

# AI Service Configuration
AI_SERVICE_URL=https://firsthand-composed-piracy-honeyandbananac.replit.app

# WhatsApp Business API (for future integration)
WHATSAPP_TOKEN=
WHATSAPP_VERIFY_TOKEN=honey-webhook-verify
WHATSAPP_PHONE_NUMBER_ID=

# Facebook Messenger (for future integration)
FACEBOOK_PAGE_ACCESS_TOKEN=
FACEBOOK_APP_SECRET=
FACEBOOK_VERIFY_TOKEN=honey-fb-verify
FACEBOOK_APP_ID=

# Email Configuration (for notifications)
# Setup guide:
# 1. For Gmail: Use app-specific password (https://myaccount.google.com/apppasswords)
# 2. For other providers: Use your regular SMTP credentials
# 3. Set EMAIL_FROM to match SMTP_USER or a verified sender email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@honey-health.com

# File Upload Configuration
MAX_FILE_SIZE=5MB
UPLOAD_DEST=./uploads

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Session Configuration
SESSION_SECRET=honey-session-secret-2024

# Redis Configuration (for future caching/sessions)
REDIS_URL=redis://localhost:6379

# Production Environment Variables (will be set by Heroku)
# DATABASE_URL (set by Heroku Postgres addon)
# PORT (set by Heroku)
# NODE_ENV=production (set by Heroku)
